import type { Metadata } from "next";

export const metadata: Metadata = { title: "Set up — Blocky" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
